module.exports = {
    theme: {
        extend: {
            fontFamily: {
                sans: ['Inter', 'sans-serif'], // for UI and headings
                serif: ['Merriweather', 'serif'], // for article content
            },
        },
    },
    plugins: [],
};